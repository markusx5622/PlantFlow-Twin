import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Engine code uses .js extensions in imports — Next.js resolves them fine with bundler moduleResolution.
};

export default nextConfig;
